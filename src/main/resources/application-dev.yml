spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/aifishing
    username: aifishing
    password: aifishing
  jpa:
    show-sql: false

app:
  guidance:
    runtime-mode: OPENAI
  seed:
    enabled: true
  admin:
    enabled: true
