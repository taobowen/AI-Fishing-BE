package com.aifishing.guidance.contracts;

public enum LearningJobType {
    ATTRIBUTE_OUTCOME,
    AGGREGATE_EMPIRICAL,
    SESSION_SUMMARY,
    REFLECTION_EVAL,
    PREFERENCE_UPDATE,
    ONLINE_METRICS_ROLLUP
}
